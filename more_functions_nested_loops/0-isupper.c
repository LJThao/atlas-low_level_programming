#include "main.h"

/**
 * _isupper - Checks for uppercase character
 * @x: The number to be checked
 * Return: Always 0
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
		return (0);
}
