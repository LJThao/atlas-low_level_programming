#include "main.h"
#include <stdio.h>

/**
 * main - print its name
 * @argc: argument c
 * @argv: argument v
 * Return: Always (0)
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%s\n", argv[0]);
}
	return 0;
}
