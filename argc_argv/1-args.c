#include "main.h"
#include <stdio.h>

/**
 * main - prints the # of arguments
 * @argc: argument c
 * @argv: argument v
 * Return: Always
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0];
	
	return (0);
}
