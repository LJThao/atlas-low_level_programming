#include "main.h"

/**
 * _isupper - Checks for uppercase character
 * @u: The uppercase character to check
 *
 * Return: Always 0
 */

int _isupper(int u)

{
	if (u >= 65 && u <= 90)
	{
		return (1);
	}
		return (0);
}
